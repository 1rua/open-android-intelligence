{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://open-android-intelligence.dev/schemas/gateway/v2/envelope.schema.json",
  "$defs": {
    "opaqueId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9._~-]+$"
    },
    "utcMillis": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}\\.[0-9]{3}Z$"
    },
    "bareSha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "prefixedSha256": {
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "nonEmptyString": {
      "type": "string",
      "minLength": 1
    },
    "positiveInt": {
      "type": "integer",
      "minimum": 1
    },
    "base64urlEd25519PublicKey": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{42}[AEIMQUYcgkosw048]$"
    },
    "base64urlEd25519Signature": {
      "type": "string",
      "pattern": "^[A-Za-z0-9_-]{85}[AQgw]$"
    },
    "success": {
      "type": "object",
      "additionalProperties": false,
      "required": ["requestId", "correlationId", "protocol", "data"],
      "properties": {
        "requestId": { "$ref": "#/$defs/opaqueId" },
        "correlationId": { "$ref": "#/$defs/opaqueId" },
        "protocol": { "const": "2.1" },
        "data": { "type": "object" }
      }
    },
    "failure": {
      "type": "object",
      "additionalProperties": false,
      "required": ["requestId", "correlationId", "protocol", "error"],
      "properties": {
        "requestId": { "$ref": "#/$defs/opaqueId" },
        "correlationId": { "$ref": "#/$defs/opaqueId" },
        "protocol": { "const": "2.1" },
        "error": {
          "type": "object",
          "additionalProperties": false,
          "required": ["code", "message", "retryable", "retryAfterSeconds", "details"],
          "properties": {
            "code": { "type": "string", "minLength": 1 },
            "message": { "type": "string" },
            "retryable": { "type": "boolean" },
            "retryAfterSeconds": {
              "anyOf": [
                { "type": "integer", "minimum": 0 },
                { "type": "null" }
              ]
            },
            "details": { "type": "object" }
          }
        }
      }
    }
  }
}
